Frontend (React + Tailwind)

Setup:

```bash
cd frontend
npm install
npm run dev
```

Environment:

Copy `.env.example` to `.env` and fill the `VITE_*` variables before running.

Deploy:

- Vercel: Create a new project pointing to this repo's `frontend` folder. Set the Vercel environment variables matching `.env`.

